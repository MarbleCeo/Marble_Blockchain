export default defineAppConfig({
  ui: {
    primary: 'red',
    gray: 'neutral',
    notifications: {
      // Notification system configuration
      position: 'top-right'
    },
    modal: {
      // Modal configuration for RPC selector
      background: 'bg-gray-900/80'
    }
  }
})

